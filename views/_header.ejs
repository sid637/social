<header>
    Page Header
    <ul>
        <!--    locals.user and user are the same thing, we are doing this so that we not have to handle the error which says user 
            variable not found  because the property can be nill but the variable cannot be just absent-->
      
        <% if (locals.user) { %>
        <li>
            <a href="/users/profile">
                <%= user.name %>
            </a>
        </li>
        <li>
            <a href="/users/sign-out">Sign Out</a>
        </li>

        <% }else{ %>

            <li>
                <a href="/users/sign-in">Sign In</a>
            </li>

            <li>
                <a href="/users/sign-up">Sign Up</a>
            </li>


            <% } %>

    </ul>
</header>